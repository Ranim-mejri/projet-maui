<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1112.view.NewPage2"
             Title="NewPage2">

  

    <StackLayout Padding="20" Spacing="10">
        <Label 
        Text="Composer votre ration :"
        VerticalOptions="Center" 
        HorizontalOptions="Center" />

        <Button Text="Choisir un fourage existant dans la liste" Command="{Binding NavigateToNewPage3Command}" />
        <Button Text="Saisir un fourage grace à vos résultats" />
    </StackLayout>
</ContentPage>

    